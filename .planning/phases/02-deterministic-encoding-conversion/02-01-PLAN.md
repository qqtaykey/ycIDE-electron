---
phase: 02-deterministic-encoding-conversion
plan: 01
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/migration/convert-encoding.mjs
  - tests/migration/encoding-conversion.spec.mjs
  - tests/migration/fixtures/encoding-conversion/README.md
  - package.json
  - .planning/phases/02-deterministic-encoding-conversion/README.md
autonomous: true
requirements:
  - ENCD-01
  - ENCD-02
  - ENCD-03
  - ENCD-04
must_haves:
  truths:
    - "Maintainer can run one deterministic command that processes unmigrated libraries in baseline order and rewrites GBK/UTF-8 targets into canonical UTF-8 outputs (ENCD-01, D-02, D-04, D-16)."
    - "Workflow blocks mixed/uncertain/high-risk files and surfaces explicit reasons instead of silently rewriting them (ENCD-02, D-01, D-07)."
    - "Each library emits a report with required counters, blocked reasons, and fixed sample list; failures in one library do not stop later libraries (ENCD-03, D-03, D-09, D-11, D-13, D-14)."
    - "Phase summary gate fails when blocked files exist or baseline reconciliation mismatches are detected (ENCD-03, ENCD-04, D-10, D-12)."
    - "Representative semantic invariants and rerun determinism checks pass for converted outputs (ENCD-04, D-02)."
  artifacts:
    - path: "scripts/migration/convert-encoding.mjs"
      provides: "Deterministic baseline-driven conversion engine with blocker-first decision matrix and strict phase gating"
    - path: "tests/migration/encoding-conversion.spec.mjs"
      provides: "Automated ENCD-01..04 contract tests covering deterministic rerun, blocker handling, reporting, and semantic invariants"
    - path: "tests/migration/fixtures/encoding-conversion/README.md"
      provides: "Fixture matrix definition for gbk/utf-8/mixed/high-risk scenarios used by tests"
    - path: "package.json"
      provides: "Runnable command surface for conversion and verification"
    - path: ".planning/phases/02-deterministic-encoding-conversion/README.md"
      provides: "Operator workflow and acceptance gate instructions for Phase 2 execution"
  key_links:
    - from: "scripts/migration/convert-encoding.mjs"
      to: ".planning/baselines/inventory-baseline.json"
      via: "fixed baseline queue from unmigrated libraries"
      pattern: "baseline.libraries.filter\\(.*!row.isMigrated\\)"
    - from: "scripts/migration/convert-encoding.mjs"
      to: "scripts/migration/classify-encoding.mjs"
      via: "classification before rewrite/block action"
      pattern: "classify(File)?Encoding"
    - from: "scripts/migration/convert-encoding.mjs"
      to: ".planning/phases/02-deterministic-encoding-conversion/reports/libraries/*.json"
      via: "per-library report writer"
      pattern: "total|scanned|converted|skipped|blocked"
    - from: "tests/migration/encoding-conversion.spec.mjs"
      to: "scripts/migration/convert-encoding.mjs"
      via: "CLI/integration assertions for ENCD contracts"
      pattern: "ENCD-0[1-4]"
---

<objective>
Deliver a deterministic, blocker-first encoding conversion pipeline for Phase 2 that converts eligible GBK/UTF-8 files to canonical UTF-8, blocks risky files, and emits strict per-library/phase reports.

Purpose: Satisfy ENCD-01..04 with auditable, repeatable outputs and zero silent corruption.
Output: Conversion engine, ENCD test suite with fixtures, package commands, and phase runbook.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-deterministic-encoding-conversion/02-CONTEXT.md
@.planning/phases/02-deterministic-encoding-conversion/02-RESEARCH.md
@.planning/phases/02-deterministic-encoding-conversion/02-VALIDATION.md
@.planning/phases/01-inventory-baseline-lock/01-01-SUMMARY.md
@scripts/migration/inventory-baseline.mjs
@scripts/migration/classify-encoding.mjs

<interfaces>
From scripts/migration/inventory-baseline.mjs:
```javascript
export async function generateInventoryBaseline({ repoRoot = DEFAULT_REPO_ROOT } = {}) { ... }
export async function validateBaselineManifest(baseline) { ... }
```

From scripts/migration/classify-encoding.mjs:
```javascript
export async function classifyEncodingState(libraryDir) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 ENCD contract tests, fixture matrix, and command surface</name>
  <read_first>
    - .planning/phases/02-deterministic-encoding-conversion/02-CONTEXT.md
    - .planning/phases/02-deterministic-encoding-conversion/02-RESEARCH.md
    - .planning/phases/02-deterministic-encoding-conversion/02-VALIDATION.md
    - package.json
  </read_first>
  <files>tests/migration/encoding-conversion.spec.mjs, tests/migration/fixtures/encoding-conversion/README.md, package.json</files>
  <behavior>
    - ENCD-01: same baseline + same input rerun produces byte-identical output hash and report hash (D-02, D-16).
    - ENCD-02: mixed/uncertain/high-risk files are blocked and never rewritten (D-01, D-07).
    - ENCD-03: per-library report contains `total/scanned/converted/skipped/blocked`, blocked reasons, and deterministic sample list size N=5 (D-09, D-11, D-13).
    - ENCD-04: converted sample files satisfy semantic invariants (line-count/token checksum/no U+FFFD replacement chars).
  </behavior>
  <action>Write `tests/migration/encoding-conversion.spec.mjs` in node:test style with test names prefixed `ENCD-01`..`ENCD-04`; assert default sample size is fixed at 5 per library (per discretion concretization of D-11). Add fixture-matrix documentation in `tests/migration/fixtures/encoding-conversion/README.md` that defines gbk/utf-8/mixed/high-risk fixtures and expected outcomes. Update `package.json` scripts to include exact commands: `encoding:convert` (`node scripts/migration/convert-encoding.mjs --write`), `encoding:check` (`node scripts/migration/convert-encoding.mjs --check`), and `test:migration:encoding` (`node --test tests/migration/encoding-conversion.spec.mjs`). Keep RED state explicit before engine implementation.</action>
  <verify>
    <automated>node --test --test-name-pattern "ENCD-01|ENCD-02" tests/migration/encoding-conversion.spec.mjs && npm run test:migration:encoding</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:migration:encoding` fails with missing conversion implementation (RED), and output includes ENCD-01..ENCD-04 test names.
    - `node -e "const p=require('./package.json'); console.log(['encoding:convert','encoding:check','test:migration:encoding'].every(k=>k in p.scripts))"` prints `true`.
    - `rg -n "N=5|sample size.*5|ENCD-0[1-4]" tests/migration/encoding-conversion.spec.mjs tests/migration/fixtures/encoding-conversion/README.md` returns matches.
  </acceptance_criteria>
  <done>Executable RED contracts exist for ENCD-01..04 with fixed sampling policy and deterministic command entrypoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic conversion engine with strict blocker policy and report reconciliation</name>
  <read_first>
    - tests/migration/encoding-conversion.spec.mjs
    - scripts/migration/inventory-baseline.mjs
    - scripts/migration/classify-encoding.mjs
    - .planning/phases/02-deterministic-encoding-conversion/02-CONTEXT.md
  </read_first>
  <files>scripts/migration/convert-encoding.mjs</files>
  <behavior>
    - Baseline queue uses unmigrated libraries in baseline list order and continues after failed library (D-13, D-14, D-16).
    - Inclusion policy is blacklist-first, includes extensionless text files, excludes third_party/dist/out and binary/unsafe candidates (D-05, D-06, D-08).
    - Decision matrix: utf-8 => canonical UTF-8 rewrite; gbk => GBK decode then UTF-8 write; mixed/uncertain/high-risk => blocked with reason (D-01, D-02, D-04, D-07).
    - Library status is `failed` if any blocked files exist even when some files converted (D-03).
    - Reports must include required counters, blocked reason list, fixed sampled files, and fail-fast on baseline mismatch (D-09, D-11, D-12).
    - Phase gate passes only when blocked==0 and all convertible targets succeeded (D-10).
  </behavior>
  <action>Implement `scripts/migration/convert-encoding.mjs` as a deterministic CLI using baseline manifest as authoritative scope, sorted relative file paths (`localeCompare('en')`), and iconv-lite for GBK decoding/UTF-8 encoding per D-02. Implement explicit high-risk mojibake signature table (for D-07) and block with machine-readable reason codes; do not attempt auto-repair. Provide `--write` default conversion mode (no mandatory dry-run per D-15) and `--check` verification mode. Emit per-library JSON reports under `.planning/phases/02-deterministic-encoding-conversion/reports/libraries/` and phase summary at `.planning/phases/02-deterministic-encoding-conversion/reports/phase-summary.json`. Enforce reconciliation invariant `scanned == converted + skipped + blocked` and baseline math alignment; throw hard error on mismatch (D-12).</action>
  <verify>
    <automated>npm run test:migration:encoding</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:migration:encoding` passes all ENCD-01..04 tests (GREEN).
    - `node scripts/migration/convert-encoding.mjs --check` exits 0 only when strict gate conditions are satisfied; exits non-zero when reconciliation mismatch is injected by test fixtures.
    - `rg -n "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|D-10|D-11|D-12|D-13|D-14|D-15|D-16|blocked|phase-summary" scripts/migration/convert-encoding.mjs` returns policy-linked logic/comments.
  </acceptance_criteria>
  <done>Conversion engine is deterministic, blocker-first, baseline-reconciled, and fully satisfies ENCD execution/reporting semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Publish Phase 2 runbook and execute phase verification commands</name>
  <read_first>
    - scripts/migration/convert-encoding.mjs
    - .planning/phases/02-deterministic-encoding-conversion/02-VALIDATION.md
    - .planning/ROADMAP.md
  </read_first>
  <files>.planning/phases/02-deterministic-encoding-conversion/README.md</files>
  <action>Create a concise runbook documenting deterministic execution order, strict gate semantics, report paths, and failure triage steps (blocked-file handling/manual remediation queue) aligned to D-03, D-10, D-12, D-14. Include exact operator sequence: `npm run encoding:convert`, `npm run encoding:check`, `npm run test:migration:encoding`, and `npm run test:migration && npm run test:migration:encoding`. Explicitly state Phase 2 boundary: no x64 adaptation, ycIDE integration, or promotion work.</action>
  <verify>
    <automated>node --test --test-name-pattern "ENCD-03|ENCD-04" tests/migration/encoding-conversion.spec.mjs && npm run encoding:check && npm run test:migration:encoding</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-deterministic-encoding-conversion/README.md` exists and contains all four required command lines verbatim.
    - `rg -n "D-03|D-10|D-12|D-14|Phase 2 boundary|no x64|no integration|no promotion" .planning/phases/02-deterministic-encoding-conversion/README.md` returns matches.
    - Verification command exits 0 after implementation is complete.
  </acceptance_criteria>
  <done>Phase 2 execution is operator-ready with explicit gates, deterministic workflow, and in-scope-only guidance.</done>
</task>

</tasks>

<verification>
- Fast smoke feedback: `node --test --test-name-pattern "ENCD-01|ENCD-02" tests/migration/encoding-conversion.spec.mjs` (quick regression signal before full run).
- `npm run test:migration:encoding` passes.
- `npm run encoding:convert && npm run encoding:check` completes with deterministic report outputs.
- `npm run test:migration && npm run test:migration:encoding` passes as phase gate.
</verification>

<success_criteria>
1. ENCD-01: Repeatable conversion produces stable outputs/reports across reruns using baseline order.
2. ENCD-02: Mixed/uncertain/high-risk files are blocked and recorded, never silently rewritten.
3. ENCD-03: Per-library reports and phase summary include required counters, blocked reasons, deterministic samples, and reconciliation checks.
4. ENCD-04: Semantic invariants for converted samples and strict phase check both pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-encoding-conversion/02-01-SUMMARY.md`
</output>
